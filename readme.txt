具体步骤：
1. 邻居向量 归一化后 求和 决定：方向
   邻居向量向量直接求和，用所得向量的模长来决定 最后的移动步长
2. finalVec = normalize(distVec) * dist(distVecNoNormalize) / neighborNum)
3. 死亡，必须从外部到内部这样的顺序来进行：
   对所有的点按照之前移动的步长进行降序排序，我们认为finalVec大的点更可能是边界点，所以finalVec越大越优先死亡（被代表）
4. 归属，点应该尽量由外层归属到内层
   所以，我们会在满足条件的点中找到distVec最小的点进行归属
5. 并查集，实现归属
6. 第二步中的聚类，去噪声点部分变成了去掉各家族人数之和最小的那些类
